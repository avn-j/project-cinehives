/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "gqzjochbuoldsohwexvu.supabase.co",
      },
    ],
  },
};

export default nextConfig;
